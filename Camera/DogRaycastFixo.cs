using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DogRaycastFixo : MonoBehaviour
{
    //vou usar esse script pra fazer o raycast q sai da camera fixa. É quase igual o do dog, mas com umas poucas diferenças

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
