using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class FarejarBar : MonoBehaviour
{
    /*
    public float farejaDur = 100f;
    public float uso = 5f;
    public float regenera = 0.5f;

    public GameObject HUD_vida;
    //public ProgressBar barraProgresso;

    void Update()
    {
        if(!CanvasManager.jogoPausado)
        {
            farejaDur -= Time.deltaTime * uso;
        }

        if(farejaDur < 100)
        {
            farejaDur += Time.deltaTime * regenera;
        }

        //barraProgresso.current = farejaDur;

        if(farejaDur <= uso)
        {
            //não pode farejar
        }
    }*/
}